{
  "name": "meelad-ai-studio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js",
    "lint": "tsc --noEmit",
    "migrate:scope": "node scripts/backfill-institution-scope.mjs",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "audit:tenant": "rg -n \"collection\\(db, ['\\\"](users|userInvitations|students|appSettings)['\\\"]\\)|doc\\(db, ['\\\"](users|userInvitations|students|appSettings|institutionLogins)['\\\"]\" admin assets/js public team judge --glob '!my old project/**'",
    "migrate:passwords": "node scripts/migrate-manual-password-hashes.mjs"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
